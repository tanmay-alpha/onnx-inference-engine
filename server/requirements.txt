# Crucible FastAPI server — production runtime dependencies
#
# Pinned to the versions used during initial development on
# Python 3.11. Pinning matters here because (a) FastAPI has
# shipped breaking changes between minor versions, and (b) torch
# wheels are ABI-tied to the Python version, so re-resolving on
# a different interpreter will fail at install time.
#
# `crucible_py` (the C++ pybind11 extension) is built separately
# via `cmake --build build/release` and added to PYTHONPATH at
# runtime. It is NOT in this file because pip can't build it
# from a wheel.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.32  # for multipart/form-data parsing

# Validation
pydantic==2.8.2
pydantic-settings==2.3.4

# Database — async ORM + migrations
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0          # async SQLite (dev)
asyncpg==0.30.0            # async PostgreSQL (prod)
alembic==1.13.2            # database migrations
bcrypt==4.2.1              # password hashing
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4     # password hashing backend

# ONNX + ML
onnx==1.22.0
torch==2.3.0
scikit-learn==1.5.2
xgboost==2.1.2
imbalanced-learn==0.12.3   # SMOTE for class imbalance

# Numerics
numpy==1.26.4

# HTTP client (used by TestClient indirectly via httpx)
httpx==0.27.0

# Structured logging
structlog==24.4.0

# Prometheus metrics
prometheus-client==0.21.0

# HTTP client (used by webhooks.py)
requests==2.32.3
