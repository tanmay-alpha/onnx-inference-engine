# Crucible FastAPI server — runtime dependencies (Issue #13)
#
# Pinned to the versions used during initial development on
# Python 3.11. Pinning matters here because (a) FastAPI has
# shipped breaking changes between minor versions, and (b) torch
# wheels are ABI-tied to the Python version, so re-resolving on
# a different interpreter will fail at install time.
#
# `crucible_py` (the C++ pybind11 extension from Issue #12) is
# built separately via `cmake --build build/release` and added
# to PYTHONPATH at runtime. It is NOT in this file because
# pip can't build it from a wheel.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.32  # for multipart/form-data parsing

# Validation
pydantic==2.8.2

# ONNX + PyTorch for /convert
onnx==1.22.0
torch==2.3.0

# Numerics
numpy==1.26.4

# HTTP client (used by TestClient indirectly via httpx)
httpx==0.27.0

# --- dev / test only -------------------------------------------------------
# Pin dev deps in requirements-dev.txt; keeping them out of the
# production file means a `pip install -r requirements.txt` in
# the Docker image doesn't drag in pytest.
pytest==9.0.3